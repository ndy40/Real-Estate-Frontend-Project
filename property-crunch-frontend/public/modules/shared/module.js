/*global define */
/**
 * Module definition file.
 */

(function (define) {
    'use strict';

    define([
        "angular"
    ], function (ng) {
        return ng.module("PCSHARED", []);
    });

}(define));
